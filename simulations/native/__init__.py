"""
Native function implementations for simulation.

This package provides mock implementations of native functions that would
normally require external system access (SSH, network checks, etc.).

In production, these would be replaced by real implementations.
"""
